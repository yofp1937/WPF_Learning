<Window x:Class="DockPanel_Learning.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DockPanel_Learning"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Button Content="좌측 버튼" DockPanel.Dock="Left"></Button>
            <Button Content="우측 버튼1" DockPanel.Dock="Right"></Button>
            <Button Content="우측 버튼2" DockPanel.Dock="Right"></Button>
            <Button Content="상단  버튼" DockPanel.Dock="Top"></Button>
            <Button Content="하단 버튼" DockPanel.Dock="Bottom"></Button>
            <Button Content="기본 버튼"></Button>
        </DockPanel>
        <DockPanel Grid.Row="1" LastChildFill="False">
            <Button Content="상단  버튼" DockPanel.Dock="Top"></Button>
            <Button Content="좌측 버튼" DockPanel.Dock="Left"></Button>
            <Button Content="우측 버튼1" DockPanel.Dock="Right"></Button>
            <Button Content="하단 버튼" DockPanel.Dock="Bottom"></Button>
            <Button Content="우측 버튼2" DockPanel.Dock="Right"></Button>
            <Button Content="기본 버튼"></Button>
        </DockPanel>
    </Grid>
</Window>
